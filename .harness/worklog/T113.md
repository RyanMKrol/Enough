# T113 — Wire up onboarding's "Browse all {country} decks" link

## 2026-07-20

### What I did
- `Sources/Features/Onboarding/PlanView.swift`: `browseLink(country:)` now wraps its existing
  HStack (magnifyingglass + "Browse all \(country.name) decks" in `EnoughColor.linkBlue`,
  visual treatment unchanged) in a `Button` that toggles `@State private var showBrowseSheet`
  and presents `BrowseView(countryId: country.id)` via `.sheet`. Added accessibility identifier
  `"plan-browse-link"` on the tappable link. Removed the stale `// Browse tab arrives in T055`
  comment.
- `Sources/Features/Browse/BrowseView.swift`: added an optional `countryId: String?` property
  (defaults to `nil`, so the existing main-tab `BrowseView()` call in `MainShellView` and the
  `#Preview` are unaffected). When `countryId` is set, an overlay dismiss (xmark) button appears
  top-trailing, using `@Environment(\.dismiss)` to close the presenting sheet — identifier
  `"browse-sheet-done"`, accessibility label "Done". Styled to match the existing session-close
  affordance (`SessionChrome.swift`).
- `Sources/Features/Browse/BrowseViewModel.swift`: added `countryId: String?` init param. When
  set, `refresh()` filters `countries` to just that one country instead of the full catalog /
  active-trip-first ordering.

### Checks run
- `swift-format lint --recursive --strict Sources Tests` — clean.
- `swiftlint lint --strict` — 0 violations, 0 serious (150 files).
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=Enough-Sim'` — **BUILD SUCCEEDED**, 245/245 tests passed, 0 failures.
- Visual verification via `./build_run.sh` on `Enough-Sim`, then drove the UI with `axe`/`xcui`
  through onboarding (Get started → Japan → Build my plan → plan screen). Tapped
  `plan-browse-link`: a sheet titled "Browse" presented, showing ONLY the Japan section (bundles
  + 4 packs, no France/Germany sections) with an X close button top-right. Tapped `Done` (by
  label — `axe --id browse-sheet-done` didn't resolve, `--label "Done"` did) and the sheet
  dismissed cleanly back to the onboarding plan screen (Step 3 of 3, still showing the Weekend
  bundle selected) — no dead end. Screenshots observed at each step.

### What remains
Nothing outstanding for this task.
