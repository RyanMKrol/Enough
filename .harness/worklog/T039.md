# T039 — Debug setting: time travel (shift the app clock by N days)

**2026-07-16** — Built cold from spec.

Did:
- Created `Sources/Services/TimeTravel.swift` with three static functions: `offset(in:)` returns the
  current day offset or nil if the provider isn't adjustable, `setOffset(_:in:)` updates the offset
  (no-op if not adjustable), and `label(for:)` returns human-readable labels ("today", "+3 days",
  "-2 days").
- Created `Sources/Features/Debug/DebugTimeTravel.swift` with a DebugSection containing a stepper row
  that wires the TimeTravel accessors with the right callbacks.
- Updated `Sources/Features/Debug/DebugRegistry.swift` to append `DebugTimeTravel.section` to the
  `DebugProviders.all` array.
- Created `Tests/TimeTravelTests.swift` with 8 assertions: positive and negative offset shifts,
  label formatting for 0/-2/+3 days, getter/setter behavior, and offset persistence via UserDefaults.

Checks run:
- `xcodegen generate` — succeeded.
- `swift-format lint --recursive --strict Sources Tests` — passed.
- `swiftlint lint --strict` — passed (with appropriate force_try disable for test setup).
- `xcodebuild test` (all 202 tests including new 8 TimeTravelTests) — all passed.

Outcome: done.
