## 2026-07-16 — T064

Rewrote `README.md` to describe the app as it now stands, per spec:
- Top paragraph corrected to the real pricing model (£1/pack + bundles, no "£3 each").
- Added "What's implemented" prose covering onboarding, tab shell, learn/review sessions,
  reviews/browse tabs, progress components, streaks, notifications, and the debug menu +
  both launch arguments (`-debug-menu`, `-demo-state`) — cross-checked against
  `.harness/tracking/TASKS.json` statuses (e.g. flagged T054 progress-tab-assembly and T102
  force-cards-due-now as still pending rather than claiming them done).
- Added "Architecture" section mirroring `architecture.md`'s module table.
- Added "Content" section describing the bundled fixture per design-spec §8.1.
- Replaced the old task table + "Status" prose with one paragraph pointing at
  `.harness/tracking/TASKS.json` / the dashboard as the live record.
- Preserved the harness pointers (HARNESS.md, CLAUDE.md) and the three Development workflows
  (xcodegen generate, ./build_run.sh, LOCAL_DOD pointer) verbatim.

Checks run: diff is scoped to `README.md` only (confirmed via `git status`); no code changed,
so no format/lint/build/test run was required for this doc-only task.

Nothing remains for this task.
