# T042 — Debug setting: seed demo state (mid-trip fixture, also via launch argument)

## 2026-07-16

Built cold from the task spec.

**What I did**
- `Sources/Services/DemoSeeder.swift`: `DemoSeeder.seed(content:context:now:)` wipes all data
  (`AppReset.wipeAll`) then writes the deterministic mid-trip fixture: a japan/weekend trip
  starting `now - 1 day` (day 2), the `jp-weekend` bundle entitlement, SRS rows for the first
  30 `jp-greetings` cards (5 mastered/review+30d, 18 review+7d, 7 due review+3d) and the first
  8 `jp-ordering-food` cards (5 due learning, 3 upcoming learning), plus 6 `DailyActivityRecord`
  rows for the 6 days ending yesterday.
- `Sources/Features/Debug/DebugDemoSeed.swift`: new `DebugDemoSeed.section` ("Demo") with a
  "Seed demo state" action row that runs the seeder against the live services and toasts the
  spec'd message.
- `Sources/Features/Debug/DebugRegistry.swift`: appended `DebugDemoSeed.section` to
  `DebugProviders.all`, before the "Danger zone" (reset) section.
- `Sources/App/LaunchArguments.swift`: new `LaunchArguments.handle(services:)`, seeds the
  fixture when `-demo-state` is present in `ProcessInfo.processInfo.arguments`.
- `Sources/App/AppState.swift`: stores the `services` it was built with and adds
  `refreshFromStore()`, which re-derives `phase`/`activeAccent` from the trip store. Needed
  because `AppState` is constructed (and its phase decided) before `RootView.init` runs.
- `Sources/App/RootView.swift`: `init` now calls `LaunchArguments.handle(services:)` then
  `appState.refreshFromStore()`, so a `-demo-state` launch seeds and lands on the main phase
  before the first frame renders. (Could not touch `Sources/EnoughApp.swift` — out of this
  task's scope — so this is done via `AppState`/`RootView`, both in scope, rather than
  reordering construction in the app entry point.)
- `Tests/DemoSeederTests.swift`: in-memory `ModelContainer` + the real bundled `ContentStore`,
  asserts (a) `DeckProgressService.totalDue() == 12`, (b) `StatsService.currentStreak() == 6`,
  (c) `TripStore.dayNumber(now:) == 2`, (d) `EntitlementStore.ownedDeckIds(catalog:) ==
  ["jp-greetings", "jp-ordering-food", "jp-at-the-bar"]`, (e) seeding twice yields exactly 38
  `CardSRSRecord`s and 6 `DailyActivityRecord`s (no duplication).

**Checks run**
- `swift-format lint --recursive --strict Sources Tests` — clean.
- `swiftlint lint --strict` — 0 violations (130 files).
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination
  'platform=iOS Simulator,id=9481593E-90BC-4051-9F45-01CAE6D17C61'` — full suite green, 204
  tests, 0 failures, including the new `DemoSeederTests`.

**What remains**
- Not run via `xcrun simctl launch ... -demo-state` on a live simulator (no Home/Reviews/Progress
  screens exist yet to visually confirm against — the task itself notes this is fine "to the
  extent those screens exist yet"). The seeded data's correctness is verified by the test above
  instead.
