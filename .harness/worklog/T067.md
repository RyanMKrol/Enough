# T067

2026-07-16

- Added `Tests/SchemaMigrationGuardrailTests.swift`: seeds one record of each of the four
  V1 model types into an on-disk store at a unique temp URL, releases the container, then
  reopens a second container against the same URL with `EnoughSchemaV1` +
  `EnoughMigrationPlan` and asserts all four records are intact. Cleans up the temp store
  directory in `tearDown`.
- Added `PersistenceStack.container(at url: URL)` in `Sources/Persistence/PersistenceStack.swift`
  to build a container at an explicit URL without changing the existing `container(inMemory:)`
  signature.
- Checks run: `swift-format lint --recursive --strict Sources Tests` (pass),
  `swiftlint lint --strict` (0 violations), `xcodegen generate && xcodebuild test -project
  Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
  (TEST SUCCEEDED, including the new test and all existing `PersistenceStackTests`).
- Nothing remaining for this task.
