# T012 worklog

## 2026-07-15

**Task:** Bundle Content/ fixture into app and implement ContentStore gateway

**Completed:**

1. Updated `project.yml` to add Content folder as a blue folder reference (type: folder, buildPhase: resources) to Enough target
2. Created `Sources/ContentKit/ContentStore.swift` with:
   - Error enum (missingResource, deckNotFound, countryNotFound)
   - Final class with bundle-aware resource loading
   - Methods: catalog(), cards(forDeck:), audioURL(forFile:), country(:), deck(:)
   - Internal caching for catalog and per-deck card data
   - Proper subdirectory handling for bundle.url lookups

3. Made ContentCatalog model Equatable to support test assertions
4. Created comprehensive `Tests/ContentStoreTests.swift` with 8 test cases covering:
   - Catalog basics (version, countries, scenarios, comingSoon)
   - Japan country structure (decks, bundles, first deck properties, bundle properties)
   - Cards loading for jp-greetings (30 cards, first card properties)
   - Audio URL resolution (valid and invalid files)
   - Deck lookup by ID with error handling
   - Country lookup errors
   - Catalog caching (calling twice returns equal values)

**Checks passed:**
- ✅ xcodegen generate succeeds
- ✅ Content/ folder is bundled inside app at Content/catalog.json
- ✅ All 8 ContentStoreTests pass
- ✅ swift-format lint passes (fixed line-length issues with guard formatting)
- ✅ swiftlint passes (fixed for-where violations)
- ✅ All 19 tests pass (existing + new ContentStoreTests)
- ✅ Verified bundle contains: Content/catalog.json, Content/decks/, Content/audio/

**Status:** Ready to commit
