# T016 — EntitlementStore persistence gateway

2026-07-15

## What I did

- Added `Sources/Persistence/EntitlementStore.swift`: `grant` (idempotent insert),
  `grantedProductIds`, `ownedDeckIds(catalog:)` (unions directly-granted deck ids with the
  expanded deck ids of any granted bundle, ignoring grants that match nothing in the catalog),
  `isOwned(deckId:catalog:)`, `revoke(productId:)`, `reset()`.
- Added `Tests/EntitlementStoreTests.swift` with an in-code `ContentCatalog` fixture (one
  country, decks `d1`/`d2`/`d3`, bundle `b1` covering `d1`+`d2`) covering: empty store, direct
  deck grant, bundle expansion, grant idempotency (row count), stale/ghost grants, revoke
  (including revoking a never-granted id), and reset.

## Checks run

- `swift-format lint --recursive --strict Sources Tests` — clean.
- `swiftlint lint --strict` — 0 violations across 32 files.
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
  — full suite, 45 tests, 0 failures (includes the 7 new `EntitlementStoreTests`).

## Remaining

- None for this task. StoreKit purchase/refund wiring (calling `grant`/`revoke` from live
  transaction updates) is future work (T062 per the spec).
