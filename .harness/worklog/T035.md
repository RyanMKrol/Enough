# T035 worklog

## 2026-07-16

**Done:** Built two presentational views for the plan screen (T036).

### What was built

1. **BundleRowView.swift** — radio-style bundle selector row
   - Struct with pinned initializer: `init(title: String, subtitle: String, price: String, isPopular: Bool, isSelected: Bool, action: () -> Void)`
   - White card (radius 18, padding 16) with row layout: 22pt radio indicator → title column → spacer → price
   - Radio indicator: selected = accent-filled circle with 8pt white dot; unselected = 1.5pt #c7c7cc ring
   - Title: 17 semibold #1c1c1e; POPULAR tag (11 bold white on accent, padding 3×7, radius 99) rendered inline with 6pt gap when `isPopular`
   - Subtitle: 13 regular #6b6b70
   - Price: 17 bold #1c1c1e
   - Selected state: 2pt accent overlay stroke + accent.opacity(0.18) shadow (radius 12, y 5)
   - Unselected state: 1pt hairline stroke #c7c7cc, no shadow
   - Entire row is one Button with light impact haptic; selection change animates with 0.3s spring

2. **PackChecklistRow.swift** — pack checklist row (static or toggleable)
   - Enum `PackRowState`: `inBundle(bundleName)`, `extra(priceLabel)`, `available(priceLabel)`
   - Struct with pinned initializer: `init(name: String, cardCount: Int, state: PackRowState, onToggle: (() -> Void)?)`
   - Row height ~52pt: 22pt indicator → name column (16 regular #1c1c1e + "X cards" 13 regular #8e8e93) → spacer → trailing status
   - Indicator: `inBundle`/`extra` = accent-filled circle with 11pt bold checkmark; `available` = 1.5pt #c7c7cc empty ring
   - Trailing: `inBundle` → bundle name (13 semibold accent); `extra` → priceLabel (13 semibold accent); `available` → priceLabel (13 semibold #6b6b70)
   - If `onToggle` is non-nil, entire row is a Button (à-la-carte toggle, light haptic); if nil, static text (bundle members)
   - No internal dividers — caller (T036) provides hairline dividers inset 49pt

### Checks run

- **Format:** `swift-format lint --recursive --strict` ✓ pass
- **Lint:** `swiftlint lint --strict` ✓ pass
- **Build:** `xcodebuild build` ✓ pass
- **Tests:** `xcodebuild test` ✓ pass (145 tests, 0 failures)

### Previews

Both views have comprehensive `#Preview` blocks:
- **BundleRowView:** selected+popular (showing accent ring, shadow, filled radio, POPULAR tag) and unselected (hairline, empty radio)
- **PackChecklistRow:** all three states (inBundle, extra, available) with correct indicator and trailing treatments; Japan accent environment

### Verification

- Both views compile with exactly the pinned initializers specified in the task (T036 binds verbatim)
- Zero business logic — no price math, no catalog access; strings and numbers flow in via init
- Previews render correctly with all visual constants matching design-spec §2.4:
  - 22pt indicators ✓
  - 17 semibold titles ✓
  - POPULAR pill (11 bold white on accent, radius 99) ✓
  - 2pt accent ring on selected bundle ✓
  - Correct stripe styling (selected vs unselected) ✓
  - Correct indicator states (filled check vs empty ring) ✓

### Status

**Done.** Both views are buildable, pass all Definition-of-Done checks, and are ready for T036 to wire them into the plan screen.
