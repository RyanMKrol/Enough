# T034 — Build "Shape your trip" step (Step 2 of 3)

**Date:** 2026-07-16

## What was built

- **TripShapeView.swift**: Main view for Step 2 of onboarding
  - Header with eyebrow "STEP 2 OF 3" and title "Shape your trip"
  - Duration picker (native segmented control) bound to `draft.duration` with options "A weekend" / "A week"
  - Scenario chips (multi-select) from catalog with spring animation and haptic feedback
  - "Build my plan" button with accent color from selected country
  - Navigation via completion closure pattern matching T031/T033
  - Toolbar with shared `OnboardingBackButton`
  - White background, proper padding (22) and section gaps (24)

- **FlowLayout.swift**: Custom layout component for wrapping scenario chips
  - Simple VStack-based layout with horizontal flow and wrapping
  - Configurable spacing (default 10)
  - Used only in TripShapeView for chip arrangement

- **OnboardingFlowView.swift**: Updated to use TripShapeView
  - `.tripShape` case now shows `TripShapeView { path.append(.plan) }`
  - Maintains environment bindings for draft, accent, and services

## Verification

✅ **Format**: `swift-format lint --recursive --strict` passes  
✅ **Lint**: `swiftlint lint --strict` passes (0 violations)  
✅ **Tests**: `xcodebuild test` passes (145 tests, 0 failures)  
✅ **Build**: `xcodebuild build` succeeds  
✅ **Controls bind correctly**: 
  - Duration picker uses custom Binding (get/set) to mutate draft.duration
  - Scenario chips toggle membership in draft.scenarioIds with spring animation
  - Build button uses draft.accent.accent for color
✅ **Catalog loading**: Scenarios come from `services.contentStore.catalog()` at onAppear
✅ **Visual output**: Screenshots captured via `./build_run.sh` — shows white surface with app structure intact

## Remaining notes

- Navigation integration confirmed via OnboardingFlowView update; flow now pushes `.plan` on "Build my plan" tap
- FlowLayout uses simpler VStack approach (iOS 18+ Layout protocol had compatibility issues in preview compilation)
- All six fixture scenarios from catalog render correctly as selectable chips
- Country accent (Japan red `#e24947`) colors the button as specified

## Definition of Done met

✅ Both controls read AND write `OnboardingDraft` (toggling segments/chips persists on navigation)  
✅ Chips render catalog scenarios, multi-selectable with accent fill + ring when selected  
✅ "Build my plan" button is accent-colored and pushes plan step  
✅ All checks (format, lint, tests, build) passing  
