# T108

## 2026-07-18

Added `.contentShape(Rectangle())` after the two `.frame(...)` modifiers in the
per-tab Button label in `Sources/DesignSystem/LiquidGlassTabBar.swift`, so the
whole expanded frame (not just the opaque glyph/text) is the hit-test area.
No other changes — layout, spacing, colours, glass effect, and shadow are
untouched.

### Checks run

- `swift-format lint --recursive --strict Sources Tests` — clean, no output.
- `swiftlint lint --strict` — 0 violations, 0 serious, 150 files.
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough
  -destination 'platform=iOS Simulator,name=Enough-Sim'` — **BUILD SUCCEEDED**,
  **TEST SUCCEEDED**, 245/245 tests passed, 0 failures.

### Visual / empirical verification

Ran `./build_run.sh`, then relaunched with `-demo-state` for a populated
mid-trip state and re-screenshotted. Bar renders visually identical to
before: same glass capsule, spacing, colours (Japan red accent), and shadow —
confirmed by screenshot comparison.

Used `axe` (installed) to read the tab bar's accessibility tree and confirm
each tab button's full frame (e.g. Browse: x=279.67 y=754 w=78.33 h=58;
Progress: x=201 y=754 w=78.67 h=58). Then drove taps deliberately offset
from the glyph/label toward each cell's edge:

- Tapped (282, 757) — top-left corner of the Browse cell, well away from the
  shopping-bag glyph/"Browse" text — selection switched to Browse
  (screenshot: `screenshots/tap_browse_edge.png`, red "Browse" glyph + label,
  Browse screen shown).
- Tapped (277, 810) — bottom-right corner of the Progress cell, away from
  the chart glyph/"Progress" text — selection switched to Progress
  (screenshot: `screenshots/tap_progress_edge.png`, red "Progress" glyph +
  label, Progress screen shown).

Both edge taps reliably switched the selected tab, confirming the whole
cell (not just the glyph/text) is now tappable. No remaining work.
