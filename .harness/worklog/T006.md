# T006 — Pulsing audio button with ripple-once replay

## 2026-07-15

### What I did

Implemented `PulsingAudioButton` component according to the spec in `Sources/DesignSystem/PulsingAudioButton.swift`:

- Created the view with the exact signature: `init(size: CGFloat = 60, isPulsing: Bool = true, action: @escaping () -> Void)`
- Built the button UI with a ZStack containing:
  - Idle pulse ring (when `isPulsing: true`): animates scale 0.85 → 1.7 and opacity 0.6 → 0 continuously using `Motion.pulse` (2.2s) with `.easeOut` and no autoreverse
  - Tap ripple ring: added dynamically on each tap, animates once over 0.8s, then is removed
  - Circle fill in `accentTheme.tint`
  - Speaker icon in `accentTheme.accent` (size `size * 0.4`, weight semibold)
- Used `@State` variables to track pulse scale/opacity and tap ripples
- Implemented tap handler that calls the provided `action()` callback and triggers a ripple animation
- Added a Preview showing both 60pt pulsing and 34pt non-pulsing instances

### Checks run

- ✅ Build: `xcodebuild build -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` → succeeded
- ✅ Tests: `xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` → 8/8 passed
- ✅ Format: `swift-format lint --recursive --strict Sources Tests` → 0 violations
- ✅ Lint: `swiftlint lint --strict` → 0 violations

### Result

**DONE** — Component compiles with correct signature, animations work as specified, all environment values read from `\.accentTheme`, preview renders both size variants.
