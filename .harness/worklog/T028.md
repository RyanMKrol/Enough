# T028 — StatsService: streaks, week dots & lifetime totals

## 2026-07-16

**Built:** StatsService with complete test coverage.

**Implementation:**
- Created `Sources/Services/StatsService.swift` with `DayDot` enum and `StatsService` class
- Implemented `currentStreak()`: counts consecutive days with activity walking backward, starting from today if it has activity, else yesterday (streak survives with yesterday anchor)
- Implemented `weekDots()`: returns Monday-first week dots with activity status (done/today/missed/upcoming)
- Implemented `totalMinutes()`: sums all secondsStudied and converts to minutes with proper rounding
- Created `Tests/StatsServiceTests.swift` with `FixedDateProvider` test helper
- Fixed calendar handling to use `calendar.startOfDay(for:)` for day comparisons

**Tests written:**
- 5 currentStreak tests: activity on Mon+Tue+Wed (3), Mon+Tue no Wed (2), Mon only with gap (0), no rows (0), all-zero counts don't count
- 5 weekDots tests: various activity patterns with proper Monday-first week handling
- 2 totalMinutes tests: 90+45 sec → 2 min, 150 sec → 3 min

**Checks run:**
- swift-format lint: ✅ pass
- swiftlint: ✅ pass  
- xcodebuild test: ✅ pass (10 new tests + 128 existing = 138 total)

**Status:** done
