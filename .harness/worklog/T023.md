# T023 — SessionEngine: pure session state machine (learn/review/practice)

## 2026-07-16

Implemented `Sources/SRSKit/SessionEngine.swift`: `SessionCard`, `SessionMode`,
`SessionSummary`, and the `SessionEngine` class per the spec — queue cursor with
`index`, `firstAttemptGrade` keyed `"deckId/cardId"`, `completedIds` set,
`submitMultipleChoice(correct:)` (learn/practice, requeues wrong answers via
`SRSQueueBuilder.requeuePosition`, clamps to `queue.count`), `submitGrade(_:)`
(review, no requeue), `advance()` (skips already-completed duplicates),
`gradedResults` (first-seen order, empty in practice mode), and `summary()`
(correct-count rules differ for learn/practice vs review; duration from the
injected `now` clock).

Added `Tests/SessionEngineTests.swift` (XCTest, `@testable import Enough`) —
written test-first against a fake clock, covering: learn session with one miss
(requeue position, grade order, `correctCount`), requeue clamp at the tail of
the queue, review grading (`submitGrade` + no-op `submitMultipleChoice`),
practice mode (empty `gradedResults`, `.practice` summary), and duration from
clock delta.

## Checks run

- `swift-format lint --recursive --strict Sources Tests` — pass
- `swiftlint lint --strict` — pass (0 violations)
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` —
  full suite, 97/97 tests pass (5 new `SessionEngineTests`)

## Remaining

None — task complete within scope (`Sources/SRSKit/**`, `Tests/**`).
