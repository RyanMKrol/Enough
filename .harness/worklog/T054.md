# T054 — Progress tab assembly (streak card, stat tiles, readiness ring)

## 2026-07-17

Assembled the Progress tab from the existing pieces:

- Added `Sources/Features/Progress/ProgressViewModel.swift` (`@Observable`): loads streak,
  week dots, words learned, minutes, decks-going (owned decks with `learned > 0`), and
  readiness percent/line from `ReadinessCalculator` fed by the active trip's scenarios
  (in stored order) and the trip country's decks. Destination label mirrors
  `ReviewsTabView`'s country→city mapping. All loads swallow-and-default on thrown errors.
- Added `Sources/Features/Progress/ProgressTabView.swift`: large title "Progress",
  `StreakCard`, three equal-width stat tiles (words learned / minutes / decks going), and
  a readiness card with a 72pt `ProgressRing` (percent shown) + the calculator's line text.
  `#Preview` with `AppServices.preview()`.
- Wired `.progress` case in `Sources/App/MainShellView.swift` to `ProgressTabView()`,
  replacing the placeholder.

## Checks run

- `swift-format lint --recursive --strict Sources Tests` — pass (one format fix applied via
  `swift-format format -i`).
- `swiftlint lint --strict` — 0 violations.
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination
  'platform=iOS Simulator,name=Enough-Sim'` — BUILD SUCCEEDED, 223 tests passed, 0 failures.
- Visual verification via `./build_run.sh`, then relaunched with `-demo-state` and navigated
  to the Progress tab via `axe tap --id tab-progress` + `xcui wait --for-element
  screen-progress`. Observed: amber streak card "6-day streak" / "Keep it alive — 3 min
  today does it" with 4 filled amber checks + outlined today ring (Fri) + 2 upcoming; stat
  tiles showing 38 words learned / 62 minutes / 2 decks going; readiness card "Tokyo survival
  readiness" with a 46% partial Japan-red ring and line "You can handle greetings.
  Directions's next." — matches design spec, no letterboxing, correct accent.

## What remains

Nothing outstanding for this task's scope.
