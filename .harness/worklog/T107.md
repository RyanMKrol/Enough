## 2026-07-17 — T107: debug reset re-routes to onboarding in place

**What I did**
- `AppState.reenterOnboardingFresh()` (`Sources/App/AppState.swift`): new method mirroring
  `startNewTrip()`'s in-place re-route (cancel notifications, `phase = .onboarding`) but with
  `isReonboarding = false`, so `RootView` lands on the very first `WelcomeView` rather than the
  country picker. Doesn't touch the stores itself — the debug reset already wiped them via
  `AppReset.wipeAll`.
- `DebugMenuView` (`Sources/Features/Debug/DebugMenuView.swift`): added `@Environment(AppState.self)`
  and, after the `reset-all` debug action completes, calls `appState.reenterOnboardingFresh()`.
  Updated the `#Preview` to inject an `AppState` into the environment (required now that the view
  reads it).
- `DebugReset.swift`: reset toast copy changed from "Data wiped — relaunch the app" to "Data wiped"
  since no relaunch is needed anymore.

**Checks run**
- `swift-format lint --recursive --strict Sources Tests` — clean.
- `swiftlint lint --strict` — 0 violations.
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=Enough-Sim'` — BUILD SUCCEEDED, 245/245 tests passed.
- Visual/empirical verification: `./build_run.sh`, then launched with `-debug-menu`, scrolled to
  "Danger zone" → tapped "Reset all data" via `axe tap --label "Reset all data"`. Observed the app
  immediately transition, with no relaunch, to the first-run `WelcomeView` ("Enough." / "Learn just
  enough of a language..." / "Get started" button) — confirmed via screenshot.

**What remains**
Nothing outstanding for this task.
