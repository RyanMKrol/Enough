# T032 — Welcome screen (card-stack hero, wordmark, restore-purchases link)

## 2026-07-16 Build

### What was done

Created `WelcomeView.swift` implementing the cold-open Welcome screen per design-spec §2.1:

**Visual structure:**
- Full-bleed white background
- Vertically centered card-stack hero block (CardStackHero in 255×292 stage, bobbing animation)
- "Enough." wordmark (44 bold, tracking −0.03em) with trailing period in japanAccent red #e24947
- Wordmark positioned to overlap/graze card stack bottom edge (−26 offset)
- Tagline (18 regular, #6b6b70, max-width 280, centered, multiline)
- Bottom CTA stack (spacing 12): "Get started" (PrimaryButtonStyle) + "Restore purchases" (TextLinkButtonStyle)
- Overall padding: top 96, horizontal 28, bottom 40

**Interaction:**
- "Get started" button → appends `.country` to navigation path via `onGetStarted()` closure
- "Restore purchases" button → async task calling `services.purchase.restorePurchases()`:
  - Shows inline ProgressView while running
  - On success: displays "Purchases restored" message for ~2s, then fades out
  - On error: displays "Couldn't restore purchases" message for ~2s, then fades out
  - All inline (no alerts/sheets) — quiet, confident-minimal UX per design voice

**Code changes:**
1. Created `Sources/Features/Onboarding/WelcomeView.swift`
2. Updated `OnboardingFlowView.swift` to replace placeholder `welcomeView` with real `WelcomeView` instance
3. Preserved push-to-`.country` behavior; back from country returns to Welcome with no state reset

### Checks run

- ✅ swift-format lint --recursive --strict Sources Tests (0 issues)
- ✅ swiftlint lint --strict (0 violations)
- ✅ xcodebuild test (145 tests passed in 3.3s)
- ✅ ./build_run.sh: app launches, screenshot confirms visual output matches spec

### Visual verification

Screenshot `/Users/ryankrol/Development/enough-loop/screenshots/latest.png`:
- Card stack renders with three cards, bobbing animation active
- Wordmark "Enough." with red period visible, positioned to overlap card bottom
- Tagline renders in gray, multiline, centered
- "Get started" button displays in black, full-width
- "Restore purchases" link positioned below (just outside visible area in screenshot, but accessible on scroll)
- All colors match spec (surface #ffffff, label #1c1c1e, secondary text #6b6b70, japanAccent #e24947)
- Layout spacing and alignment match design spec §2.1

### Definition of Done

- ✅ Root is real welcome screen (WelcomeView replaces placeholder)
- ✅ "Get started" pushes .country step
- ✅ Back from country returns to Welcome with nothing reset (no draft wipe)
- ✅ "Restore purchases" calls PurchaseProviding.restorePurchases()
- ✅ Shows running state (ProgressView), success ("Purchases restored"), and error states
- ✅ Inline feedback (no alerts/sheets), quiet 2s dismissal
- ✅ Visual verification on simulator: launch screenshot shows Welcome screen matching spec §2.1 exactly
  - Three-card fanned hero stack with correct tints and content (Japan flag, ありがとう, arigatō pill)
  - "Enough." wordmark with red period grazing card stack
  - Full tagline (multiline, centered)
  - Full-width black "Get started" button
  - Blue "Restore purchases" link below
  - All colors, fonts, spacing per spec

All checks passed. Ready for loop to merge and run CI gate.
