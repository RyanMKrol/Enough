# T019 — SRS core: card scheduling state + SM-2 grading function

## 2026-07-16

Built `Sources/SRSKit/SRSState.swift` (`SRSGrade`, `CardStatus`, `SRSState`, `.newCard`) and
`Sources/SRSKit/SRSEngine.swift` (`SRSEngine.apply(_:to:now:)`) implementing the SM-2-style
ladder per spec, split into one private helper per grade to satisfy swiftlint's cyclomatic
complexity / function body length limits. Time only arrives via the `now` parameter; the
target's default `MainActor` isolation is opted out with `nonisolated` on every SRSKit type so
the module stays synchronous/pure.

Added `Tests/SRSEngineTests.swift` (XCTest) pinning: the good/good/good/good ladder from
`.newCard` ([1, 3, 8, 20] days, `[.learning, .learning, .review, .review]`, repetitions
[1,2,3,4], dueAt each derived from now); again on a review card (lapses 0→1, repetitions→0,
→learning, interval 1/1440, ease→2.3); again on a learning card not incrementing lapses; hard
on a sub-day learning card (→1 day, ease→2.35); hard on a learning card at exactly 1 day
(holds at 1 day); hard on a review card at interval 8 (→10 days); easy on a learning card at
interval 1 (→review, 6 days, ease→2.65); and the ease floor (graded `again` repeatedly from
`.newCard` converges to and holds at 1.3 — used 10 iterations since 5 iterations only reaches
2.5 - 5×0.2 = 1.5, still above the floor).

Wrote the test first, ran it against `Sources/SRSKit` before it compiled successfully, then
implemented until green.

Checks run (all passed):
- `swift-format lint --recursive --strict Sources Tests`
- `swiftlint lint --strict`
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — 65/65 tests passed, including the 8 new `SRSEngineTests`.
- `grep -rn "^import" Sources/SRSKit/` — only `Foundation`; `grep -rn "Date()" Sources/SRSKit/` — no matches.

Nothing remains outstanding for this task.
