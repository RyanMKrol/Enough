# T001 — Project scaffold + CI green on an empty build

**2026-07-14** — Completed interactively (needs-human gate).

Did:
- Added `project.yml` (XcodeGen), `Sources/EnoughApp.swift` + `ContentView.swift` (placeholder
  screen), `Tests/EnoughTests.swift` (trivial passing test).
- Added `.swiftlint.yml` excluding `.harness/`, `Enough.xcodeproj`, `build`.
- Installed `swift-format` (brew) and reformatted sources to pass `swift-format lint --recursive
  --strict Sources Tests`.
- Wired `.harness/config/harness.env`'s `LOCAL_DOD` and `.github/workflows/ci.yml` to the same
  four steps: `xcodegen generate`, swift-format check, `swiftlint lint --strict`, `xcodebuild
  test`.
- Added `build_run.sh` (adapted from the `basket` project's script) — builds, installs, launches,
  and screenshots the app on a simulator; confirmed it works end-to-end
  (`screenshots/latest.png` shows the placeholder screen rendering correctly).

Checks run:
- `xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS
  Simulator,name=iPhone 17 Pro'` — passed.
- Full `LOCAL_DOD` chain run end-to-end locally — passed (exit 0).
- `./build_run.sh` — built, installed, launched, and screenshotted successfully.

Outcome: done.
