# T013 Worklog

## 2026-07-15

### What was done
Implemented the clock abstraction as specified:
1. Created `Sources/CoreKit/DateProvider.swift` with:
   - `DateProvider` protocol defining `var now: Date { get }`
   - `SystemDateProvider` struct that returns the current system date

2. Created `Sources/CoreKit/AdjustableDateProvider.swift` with:
   - `AdjustableDateProvider` final class for debug "time travel"
   - Persisted `dayOffset` property stored in UserDefaults under `"debug.dayOffset"`
   - `now` property that returns `Date()` offset by the stored number of days
   - Graceful fallback to `Date()` if calendar addition fails

3. Created `Tests/DateProviderTests.swift` with comprehensive hermetic tests:
   - SystemDateProvider returns current date within 5 seconds
   - Fresh AdjustableDateProvider has zero offset and current date
   - Positive offsets work correctly (tested +3 days)
   - Negative offsets work correctly (tested -2 days)
   - Persistence across instances with same UserDefaults suite
   - All tests use throwaway `UserDefaults(suiteName: "DateProviderTests")`
   - tearDown cleans up with `removePersistentDomain`

### Tests run
- Format: `swift-format lint --recursive --strict Sources Tests` ✅
- Lint: `swiftlint lint --strict` ✅
- Build/Test: `xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` ✅
  - 6 new DateProvider tests all pass
  - Full test suite: 25 tests passed, 0 failures

### Definition of Done checks
- ✅ All formatting rules pass (swift-format)
- ✅ All linting rules pass (swiftlint strict)
- ✅ All tests pass (6 new tests plus 19 existing)
- ✅ No files outside Sources/CoreKit/ and Tests/ modified
- ✅ Tests are hermetic (use throwaway UserDefaults suite)
- ✅ All "Done when" assertions implemented and passing

### Status
done
