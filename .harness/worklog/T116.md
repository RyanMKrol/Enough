
---
failed:blocked T116 — agent reported failed:blocked — global MainShellView safeAreaInset fix does not reach NavigationStack-pushed DeckDetailView (Practice button still ~54pt behind tab bar per axe frame check) — needs Sources/Features/DeckDetail/DeckDetailView.swift, out of scope

---
done (by hand, 2026-07-20) — Completed manually in an interactive session (loop stopped), not via the
loop. Root-caused the block: the floating LiquidGlassTabBar is a ZStack overlay in MainShellView that
reserves NO layout space, and DeckDetailView (pushed inside HomeView's own NavigationStack, UIKit-hosted
with its own safe area) has a bottom-pinned action bar — so the prior global `.safeAreaInset` attempt
never reached it. Fix (owner chose "reserve space, keep bar visible"): added `Layout.floatingTabBarClearance`
and applied it as bottom padding to DeckDetailView's DeckDetailActionBar so "Continue learning" / "Practice
all" lift clear of the bar. Files touched: Sources/DesignSystem/Layout.swift, Sources/Features/DeckDetail/DeckDetailView.swift
(wider than the task's original MainShellView/LiquidGlassTabBar scope — the real fix lives in the pushed
screen, exactly what blocked the loop). Verified on the simulator (Enough-Sim): both buttons sit 38pt above
the tab bar's top edge, both tappable, tab switching intact, no crashes. Full DoD green (format, lint, 243
tests). Also fixed an UNRELATED pre-existing time-bomb in NotificationsServiceTests (hardcoded 2026-07-20
09:30 due date failed once that moment passed) in a separate commit.
