# T102 Worklog

## 2026-07-17 — Initial implementation

### What I did
Implemented the debug action to make all studied cards due immediately. Followed the exact spec and resolved the scope contradiction from T040.

**Changes:**
1. Added `forceAllDue(now:Date) -> Int` method to `Sources/Persistence/CardSRSStore.swift` — fetches all non-"new" records using `FetchDescriptor`, sets `dueAt = now`, saves context, returns count.
2. Created `Sources/Services/DebugSRSTools.swift` — `enum DebugSRSTools` with `static func forceAllDue(store:now:)` that delegates to the store method.
3. Created `Sources/Features/Debug/DebugForceDue.swift` — `enum DebugForceDue` with a "Spaced repetition" section containing one `.action` row ("Make everything due now") that calls the service and toasts the result count.
4. Updated `Sources/Features/Debug/DebugRegistry.swift` — appended `DebugForceDue.section` to `DebugProviders.all` array.
5. Created `Tests/DebugSRSToolsTests.swift` — comprehensive test fixture with three records (new, learning, review):
   - Test 1: `forceAllDue` returns 2 (only non-"new" records), new record keeps nil `dueAt`
   - Test 2: `dueRecords(now:ownedDeckIds:)` returns exactly 2 records (learning + review) after forcing all due

### Checks run
- **Format**: `swift-format lint --recursive --strict Sources Tests` — passed
- **Lint**: `swiftlint lint --strict` — 0 violations
- **Test**: Full suite `xcodebuild test ... Enough` on `Enough-Sim` — 243 tests passed (including 2 new DebugSRSToolsTests)
  - Test suite time: 3.435 seconds

### Done-when checklist
✅ `CardSRSStore.forceAllDue(now:)` exists, sets `dueAt = now` on every non-"new" record, saves context, returns count  
✅ Debug menu shows "Spaced repetition" section with "Make everything due now" action that toasts "N cards due"  
✅ `Tests/DebugSRSToolsTests.swift` passes both assertions (returns 2 for fixture, dueRecords returns learning+review only)  
✅ No files outside declared scope modified  

### Result
All Definition of Done checks passed. Ready for merge.
