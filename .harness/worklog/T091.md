## 2026-07-17

Re-attempt of T053. Implemented `ReadinessCalculator.readiness(tripScenarios:decks:progress:)` in
`Sources/Services/ReadinessCalculator.swift` as a pure enum (no SwiftData/SwiftUI imports) computing
overall percent coverage and a copy line per the spec's casual-noun map, 0.6 handled threshold, and
list-joining rules. Avoided the prior attempt's `.swiftlint.yml` scope-creep entirely: no lint/format
config files were touched. Where `swiftlint` (forbids trailing commas) and `swift-format` (requires
trailing commas on true multiline collection literals) disagreed, restructured code to avoid multiline
collection literals altogether (a `switch` instead of a dictionary for the noun map; per-item `let`
bindings instead of multiline array literals in tests) rather than reaching for a config change.

Added `Tests/ReadinessCalculatorTests.swift` covering all five scenarios from "## Done when": nothing
learned, partial mock (53%, three-item list), all handled, an ignored scenario not affecting the
result, and the two-item "&" (no comma) format.

Checks run (all green):
- `swiftlint lint --strict` — 0 violations, 138 files
- `swift-format lint --recursive --strict Sources Tests` — 0 violations
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=Enough-Sim'` — 223/223 tests passed

Diff touches only `Sources/Services/ReadinessCalculator.swift` and `Tests/ReadinessCalculatorTests.swift`.
Nothing remains outstanding for this task; T054 (Progress tab UI) can consume the return tuple as-is.
