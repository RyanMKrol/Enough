# T008 worklog

## 2026-07-15 attempt 1

### What was done
Created `Sources/DesignSystem/StreakPill.swift` implementing a white pill with a flickering flame icon and bold count.

### Implementation details
- `StreakPill` struct with `init(count: Int)`
- HStack(spacing: 5) layout with flame icon and text
- Flame: `Image(systemName: "flame.fill")` in `EnoughColor.streakAmber` (#de6907), 15pt
- Continuous flicker animation: scale 1.0 ↔ 1.08 and rotation −3° ↔ +3° over 2.2s (Motion.flicker) with easeInOut and autoreverse
- Count text: 15pt bold in `EnoughColor.label`
- Pill styling: `EnoughColor.surface` background, 7px vertical / 12px horizontal padding, clipped to Capsule shape
- Subtle shadow: black 0.05 opacity, radius 3, y offset 1
- Preview block showing `StreakPill(count: 6)` on `EnoughColor.canvas` background

### Checks run
- swift-format lint: ✅ PASS
- swiftlint lint --strict: ✅ PASS (0 violations)
- xcodebuild test: ✅ PASS (8 tests, 0 failures)

### Result
**done** — All Definition of Done checks passed. StreakPill compiles with correct signature and behaves as specified.
