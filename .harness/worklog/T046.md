# T046 — MC question components worklog

## 2026-07-16 — Implementation complete

Implemented both presentational components as specified:

### MCQuestionHeader.swift
- `struct MCQuestionHeader: View` with exact initializer signature
- Centered VStack with 14pt spacing
- PulsingAudioButton (60px, pulsing ring)
- Target phrase (34 Bold, EnoughColor.label)
- Pronunciation text (15 Regular, hex #6b6b70)
- "What does this mean?" prompt (13 Semibold, tertiary text color, 6pt top padding)
- Preview with Japanese example phrase

### AnswerRow.swift
- `enum AnswerRowState: Equatable` with 5 cases (idle, correct, wrongShake, faded, correctOutline)
- `struct AnswerRow: View` with exact initializer signature
- Full-width button, min height 56, radius 15, 17 Medium text, leading-aligned, padding 14×16

#### Per-state rendering:
- `.idle` — insetSurface fill, label text
- `.correct` — successGreen fill, white text, white CheckPopView (22) with pop animation
- `.wrongShake` — #ffe9e6 fill, #b71824 text, red xmark icon, ShakeEffect animation (once per entry)
- `.faded` — insetSurface fill, #c0c0c6 text
- `.correctOutline` — white fill, 2pt successGreen border, label text, green checkmark icon

#### Behavior:
- Button disabled in all states except .idle via allowsHitTesting
- State transitions use Motion.popSpring
- Shake counter incremented in onChange(of: state) to trigger ShakeEffect exactly once
- Spring scale animation (1→1.03→1) on entering .correct state

#### Preview:
- Stack showing all five states on white canvas
- Sample English answer text

### Verification
- swift-format lint: ✅ passed
- swiftlint lint --strict: ✅ passed (0 violations)
- xcodebuild test: ✅ passed (157 tests, 0 failures)
- Build completes successfully

All requirements met per Done When criteria:
- Both files with pinned names/initializers
- All five row states render distinctly per design values
- Rows only accept taps in idle state
- Shake animation fires once per wrongShake entry
- Previews compile and cover header + all five row states
