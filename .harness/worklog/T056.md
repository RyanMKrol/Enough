# T056 — NotificationsService: review-due local notifications

## 2026-07-16

Did:
- Added `Sources/Services/NotificationsService.swift`: `UserNotificationCentering` protocol
  seam + `UNUserNotificationCenter` conformance, `NotificationsService` with
  `requestPermissionIfNeeded()` (reads live authorization status every call, never caches),
  `rescheduleReviewNotification(dueCount:nextDueDate:)` (removes then conditionally adds the
  single non-repeating "review-due" request, singular/plural body), `cancelAll()`, and the pure
  `static forecast(dueDates:now:)` helper. `NoopNotificationCenter` fake for previews/tests.
- Registered `notifications: NotificationsService` in `AppServices` — `live()` uses
  `UNUserNotificationCenter.current()`, `preview()` uses the no-op fake.
- Wired `SessionCompleteView.onAppear` to request permission then recompute + reschedule from
  owned decks' non-new `CardSRSRecord.dueAt` values.
- Wired `RootView` to observe `scenePhase` and recompute + reschedule (no permission request)
  when it becomes `.active` while `phase == .main`.
- Updated `Tests/TimeTravelTests.swift`, `Tests/HomeViewModelTests.swift`,
  `Tests/MCSessionViewModelTests.swift`, `Tests/ReviewSessionViewModelTests.swift` to pass the
  new `notifications:` argument to `AppServices.init`.
- Added `Tests/NotificationsServiceTests.swift` covering permission gating per authorization
  status (with a UserDefaults-untouched assertion), reschedule add/remove behaviour (due,
  zero-count, past-date, nil-date, singular body), and the `forecast` helper.

Checks run:
- `swift-format lint --recursive --strict Sources Tests` — clean.
- `swiftlint lint --strict` — 0 violations.
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=Enough-Sim,id=9481593E-90BC-4051-9F45-01CAE6D17C61'` —
  214/214 tests pass, including all 10 new `NotificationsServiceTests`.
- Grep gate: `requestAuthorization`/`requestPermissionIfNeeded` appear only in
  `NotificationsService.swift` and `SessionCompleteView.swift` — no launch-time prompt.

Remaining:
- No `UNUserNotificationCenterDelegate` in v1 (documented as a decided deferral in the
  service's doc comment) — tap-to-open routing to Reviews is future work.
