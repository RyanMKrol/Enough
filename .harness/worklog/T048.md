# T048 — Flashcard component worklog

## 2026-07-16

**Completed:** Built `FlashcardView` component with full 3D flip animation, front/back face layouts, and proper state management.

**Implementation:**
- Created `Sources/Features/Session/FlashcardView.swift` with signature: `init(card: CardContent, isRevealed: Binding<Bool>, onPlayAudio: @escaping () -> Void)`
- Card container: RoundedRectangle radius 28, white fill, shadow 0/20/42@14%, frame maxWidth .infinity + minHeight 380, padding 22
- Front face: VStack(spacing 18) with 60pt PulsingAudioButton (isPulsing: true) and 36pt Bold target phrase, centered
- Hint text: "Tap the card to reveal" (13 Regular, tertiary color) displayed below card, hidden when revealed
- Back face: Tinted header band (accentTheme.tint, padding 14×18) with target (20 Semibold) + pronunciation (13 Regular) both in deep color, centered; below fills rest with 22pt Semibold english phrase and 34pt non-pulsing audio button
- Flip animation: rotation3DEffect around y-axis, 0.4s easeInOut, front face opacity 1 before 90°, back face (pre-rotated 180°) after; driven by `withAnimation(.easeInOut(duration: 0.4))`
- Tap behavior: toggles isRevealed only front→back (no flip-back on second tap); programmatic reset to false renders cleanly via `.animation(nil)`
- Two #Preview variants: front (isRevealed: .constant(false)) and revealed (isRevealed: .constant(true)) using preview CardContent (ありがとうございます / arigatō gozaimasu / "Thank you very much") on #f2f2f7 canvas

**Verification:**
- Format: `swift-format lint --recursive --strict` — PASS
- Lint: `swiftlint lint --strict` — PASS (0 violations)
- Build & tests: `xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — PASS (162 tests, 0 failures)
- Previews compile and render correctly on canvas

**Status:** Done. Ready for T049 (review session view) to consume this component.
