# T117 Worklog

## 2026-07-20

**Summary:** Removed the "New trip" button and all related recurring new-trip flow elements as requested.

**Work completed:**
- Removed `@State private var showsNewTripConfirmation` from ProgressTabView.swift
- Removed the `confirmationDialog("Start a new trip?", ...)` modifier from ProgressTabView.swift
- Simplified `titleRow` to display only "Progress" text without the "New trip" button
- Removed `startNewTrip()` method from AppState.swift (~lines 58-67)
- Updated `isReonboarding` docstring to reflect its current use by debug "Reset all data" action
- Updated `reenterOnboardingFresh()` docstring to remove reference to the deleted `startNewTrip()` method
- Removed `AXID.progressNewTrip` from AXID.swift
- Removed two test methods that called `startNewTrip()`:
  - `testStartNewTripFlipsPhaseWithoutTouchingEntitlements()`
  - `testStartNewTripDeactivatesTripWipesProgressKeepsEntitlements()`
- Retained all re-onboarding machinery: `isReonboarding` property, `reenterOnboardingFresh()` method, and related onboarding flow integration (as debug "Reset all data" still depends on these)

**Verification:**
- Format check: ✓ (swift-format lint --recursive --strict)
- Lint check: ✓ (swiftlint lint --strict — 0 violations)
- Test suite: ✓ (243 tests passed, no failures)
- Build: ✓ (xcodebuild test succeeded)
- Grep verification: ✓ (no remaining references to `startNewTrip`, `showsNewTripConfirmation`, or `progressNewTrip` in source/test files)
- Visual verification: ✓ (Progress tab displays "Progress" title only, no "New trip" button, screenshot captured at progress-tab.png)

**Status:** done
