# T066 worklog

## 2026-07-16 (build attempt 1)

**Done:** Created App Store privacy manifest as required since May 2024.

**Changes:**
- Created `Sources/PrivacyInfo.xcprivacy` with:
  - `NSPrivacyTracking`: false
  - `NSPrivacyTrackingDomains`: empty array
  - `NSPrivacyCollectedDataTypes`: empty array  
  - `NSPrivacyAccessedAPITypes`: one entry for UserDefaults with reason code CA92.1 (app reads/writes its own UserDefaults for debug day-offset and notification bookkeeping)
- Added comprehensive tests in `Tests/PrivacyManifestTests.swift` to verify:
  - File is bundled in the app
  - Plist is valid XML
  - Manifest has correct structure with all required keys

**Verification:**
- Format check: `swift-format lint --recursive --strict Sources Tests` ✓
- Lint check: `swiftlint lint --strict` ✓
- Build & test: `xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` ✓ (181 tests pass)
- Plist validation: `plutil -lint Sources/PrivacyInfo.xcprivacy` ✓
- Bundle verification: PrivacyInfo.xcprivacy exists in built .app bundle ✓

**Required-reason API audit results:** Zero hits found for all APIs checked:
- `creationDate`
- `modificationDate`
- `contentModificationDateKey`
- `systemUptime`
- `ProcessInfo.processInfo.systemUptime`
- `volumeAvailableCapacity`
- `boottime`

As expected, system frameworks handle their own required-reason declarations. No additional API types needed in the manifest.

**XcodeGen resource bundling:** Verified that XcodeGen automatically treats non-source files in the Sources path as resources by default. No explicit `resources:` entry needed in `project.yml`.

## Done
