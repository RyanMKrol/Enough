# T057 worklog

## 2026-07-16

**Completed T057: Debug setting: notification test fire + pending-schedule inspector**

### What was done:
1. Extended NotificationsService with debug methods:
   - `fireTestNotification(dueCount:)` — schedules a review notification with 2-second UNTimeIntervalNotificationTrigger, returns the body text
   - `pendingSummary()` — formats pending notifications as "N pending · next ~XhYmZs" or "none pending"

2. Changed NotificationsService.center access level from `private` to default (internal) to allow access from the debug extension

3. Created `Sources/Features/Debug/DebugNotifications.swift` with two debug rows:
   - "Fire review notification in 2s" action row that gets current due count and calls fireTestNotification
   - "Pending notifications" info row that caches and displays the latest pending summary

4. Added DebugNotifications.section to the DebugProviders.all array in DebugRegistry.swift

5. Wrote comprehensive tests in `Tests/NotificationDebugToolsTests.swift`:
   - Test that fireTestNotification schedules exactly one request with 2-second trigger
   - Test that trigger identifier matches "debug.test-review-notification"
   - Test singular/plural body text handling
   - Test pendingSummary reports "none pending" when empty
   - Test pendingSummary reports count and time interval for pending notifications

### Checks run:
- ✅ swift-format lint --recursive --strict Sources Tests
- ✅ swiftlint lint --strict (0 violations)
- ✅ xcodebuild test (218 tests pass, including 4 new tests for notification debug tools)

### Verification:
All Definition of Done checks pass. The debug tools are ready for empirical testing on the simulator with notification permission granted.
