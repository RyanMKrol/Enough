# T104 — Continue card shows ring percent, meta line stays single line

## 2026-07-17 (Builder)

**What I did:**
- Changed `showsPercent: false` to `showsPercent: true` on the ProgressRing call in ContinueCard.swift (line 19)
- Added `.lineLimit(1)` and `.minimumScaleFactor(0.85)` to the detail Text (lines 32-33)
- Added `.layoutPriority(1)` to the middle VStack (line 35) so it claims width priority over the trailing Spacer

**Checks run:**
- Format lint: ✓ (no violations)
- SwiftLint: ✓ (0 violations, 150 files)
- Unit tests: ✓ (245 tests, 0 failures)
- Build: ✓ (succeeded)
- Visual verification: ✓ (ran `./build_run.sh` then `xcrun simctl launch ... -demo-state` with screenshot)

**Visual observation:**
The Continue card now displays:
- A red progress ring with "44%" centered inside (instead of no percent)
- The detail line "8 of 18 cards · about 4 min left" renders as a SINGLE line with no wrapping
- Card gradient, corner radius (24), play button (44pt red circle), and all copy strings remain unchanged

**Scope:**
Only Sources/Features/Home/ContinueCard.swift was modified — no other files touched, including build_run.sh.

**Status:** done
