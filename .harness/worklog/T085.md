# T085 — Axiom testing-auditor sweep

**2026-07-17**

## What I did

Ran the `axiom:testing-auditor` agent over the whole codebase (Sources/ and Tests/, 102
production files / 39 test files). Triaged every finding, fixed all confirmed test-quality
gaps, deferred the rest with a concrete revisit condition.

## Triage table

| # | Finding | Verdict | Notes |
|---|---|---|---|
| 1 | Entire unit-test suite lives in the app target (`EnoughTests` → `@testable import Enough`), not an extracted Swift Package — every test run pays a full app-target build + simulator launch instead of `swift test` | Deferred | Real, but a repo-wide architectural restructuring (extracting SRSKit/CoreKit/ContentKit/Persistence/Services into a local SPM package + moving ~30 test files) is far outside this task's atomic scope and outside T085's allowed `scope` (project.yml, Sources/, Tests/ only — a package split touches project structure broadly). Revisit as its own dedicated backlog task if test-run time becomes a bottleneck. |
| 2 | `Tests/EnoughTests.swift:4-6` — `testAppLaunchesWithoutCrashing` asserts `XCTAssertTrue(true)`, a tautology that can never fail | **Fixed** | Replaced with `testAppStateConstructsFromPreviewServices`, which builds `AppServices.preview()`, constructs `AppState`, and asserts `phase == .onboarding` — a real, falsifiable smoke assertion. |
| 3 | `Sources/ContentKit/ContentStore.swift:28,63` — `ContentStoreError.missingResource` thrown on missing `catalog.json` / missing deck file, but never exercised against a real `ContentStore` in tests (only reused as a synthetic mock throw elsewhere) | **Fixed** | Added `testMissingCatalogResourceThrowsMissingResource` (empty temp-dir bundle, no `Content/catalog.json`) and `testMissingDeckFileResourceThrowsMissingResource` (catalog references a deck file that doesn't exist on disk) to `Tests/ContentStoreTests.swift`, both asserting the specific `.missingResource(path)` case and path value. |
| 4 | `Sources/Features/Progress/ProgressViewModel.swift` has no test file, unlike every sibling view model (Home, Browse, MCSession, ReviewSession, Plan); `refresh()` swallows 6+ service failures via `try?` | **Fixed** | Added `Tests/ProgressViewModelTests.swift` (same `AppServices` seeding pattern as `HomeViewModelTests`) covering: (a) no active trip → readiness/destination stay empty, (b) active trip with an owned/partially-learned deck → destination + `decksGoing` populate correctly, (c) `ContentStore.catalog()` throwing (empty bundle) → `refresh()` falls back to the zeroed/empty state instead of crashing or leaving stale values. |
| 5 | `Tests/PlanViewModelTests.swift:13` — `setUp` swallows `store.country("japan")` failure via `try?`, so a real catalog regression would fail an unrelated downstream assertion with a confusing message instead of failing setup loudly | **Fixed** | Changed to `override func setUp() async throws { try await super.setUp(); ...; japan = try XCTUnwrap(try store.country("japan")) }`, matching the pattern already used correctly in `PricingCalculatorTests.swift`. (Needed `async throws`, not just `throws` — `XCTestCase.setUp()` only has non-throwing and `async throws` overridable forms.) |
| 6 | Entire 39-file suite is 100% XCTest with zero Swift Testing (`@Test`/`#expect`) adoption; several near-duplicate test methods (`TimeTravelTests`, `PricingCalculatorTests`) are natural `@Test(arguments:)` candidates | Deferred | Modernization opportunity, not a correctness or reliability gap — zero `sleep()`/shared-mutable-state/flaky-async findings were found. Revisit incrementally as new test files are added, or as a dedicated migration task; not a "fix" in the audit-sweep sense. |
| 7 | `Sources/Services/PurchaseService.swift:8-12` — `PurchaseOutcome.cancelled`/`.pending` cases exist but `Tests/StubPurchaseServiceTests.swift` only ever drives the `.success` path | Deferred | Confirmed real, but `StubPurchaseService.purchase(...)` (Sources/Services/StubPurchaseService.swift:21-26) is hardcoded to always return `.success` — there is currently no way to trigger `.cancelled`/`.pending` without changing the stub's *production* behavior (adding a configurable outcome knob), which is a behavior change beyond a test-only fix. Revisit when real StoreKit purchase flows are implemented (a likely near-term backlog item per README's IAP model) — that's when `.cancelled`/`.pending` become reachable, real caller-visible paths worth testing. |

No `sleep()`, `Thread.sleep`, shared mutable `static`/`class var` state, or flaky async
polling patterns were found anywhere in `Tests/` — the auditor's flaky-pattern scan came back
clean.

## Checks run

- `swift-format lint --recursive --strict Sources Tests` — pass (0 output).
- `swiftlint lint --strict` — 0 violations (initially 1 opening-brace-spacing violation from a
  new helper in `ContentStoreTests.swift`; fixed by reformatting the parameter list).
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination
  'platform=iOS Simulator,name=Enough-Sim'` — **234/234 tests passed** (up from 228; +6 new
  tests: 1 rewritten in `EnoughTests`, 2 new in `ContentStoreTests`, 3 new in
  `ProgressViewModelTests`), 0 failures.
  - First run surfaced two issues in the new tests themselves, both fixed before the final
    green run: (a) `PlanViewModelTests.setUp()` needed `async throws` (XCTestCase doesn't allow
    a plain-`throws` override), (b) the new `ContentStoreTests` deck-fixture helper wasn't
    creating the `Content/` subdirectory before writing `catalog.json` into it.

## What remains

Findings #1, #6, #7 above are deferred with concrete revisit conditions (see triage table) —
no further action needed for this task. A re-walk of the testing-auditor checklist over the
changed files (`EnoughTests.swift`, `ContentStoreTests.swift`, `ProgressViewModelTests.swift`,
`PlanViewModelTests.swift`) reports no remaining confirmed findings in this domain.
