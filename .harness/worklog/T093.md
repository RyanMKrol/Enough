## 2026-07-17 — T093: real Liquid Glass on tab bar + deck-detail chrome

Did:
- `Sources/DesignSystem/LiquidGlassTabBar.swift`: replaced the manual
  `.ultraThinMaterial` + white overlay + stroke Capsule background with
  `.glassEffect(.regular, in: Capsule())`. No `#available` guard — min target is
  iOS 26.
- `Sources/Features/DeckDetail/DeckDetailView.swift`: replaced the two chrome
  circle buttons' `Color.white.opacity(0.25)` + `.ultraThinMaterial` backing
  with `.glassEffect(.regular, in: Circle())`, wrapped both in a
  `GlassEffectContainer` so they composite together.

Checks run (all passed):
- `swift-format lint --recursive --strict Sources Tests`
- `swiftlint lint --strict` — 0 violations, 143 files
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=Enough-Sim'` — 239 tests, 0 failures, BUILD SUCCEEDED

Visual verification: ran `./build_run.sh`, relaunched with `-demo-state`, and
navigated (via axe/xcui accessibility taps) to the Learn tab and into the
"Ordering food" deck detail screen. Observed the floating tab bar rendering
real glass refraction/translucency over the canvas background, and the
back-chevron + ellipsis circles on the deck-detail red hero rendering the same
real glass material (visibly distinct from the old flat blur), both keeping
existing layout/sizing/copy unchanged.

Remaining: nothing — task scope fully covered.
