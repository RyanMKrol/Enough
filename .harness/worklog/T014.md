# T014 worklog

## 2026-07-15

Built the SwiftData foundation:

- `Sources/Persistence/Records.swift` — the four `@Model` classes (`TripProfileRecord`,
  `EntitlementRecord`, `CardSRSRecord`, `DailyActivityRecord`) with memberwise inits and the
  documented defaults (`isActive = true`, `easeFactor = 2.5`, `intervalDays = 0`,
  `repetitions = 0`, `lapses = 0`, `dueAt = nil`, `lastReviewedAt = nil`).
- `Sources/Persistence/EnoughSchema.swift` — `EnoughSchemaV1: VersionedSchema` registering all
  four models at version `1.0.0`, plus `EnoughMigrationPlan: SchemaMigrationPlan` with an empty
  `stages` array.
- `Sources/Persistence/PersistenceStack.swift` — `PersistenceStack.container(inMemory:)` building
  `Schema(versionedSchema: EnoughSchemaV1.self)` + a `ModelConfiguration` (random UUID name when
  `inMemory`, stable `"Enough"` name on disk) and `PersistenceStack.models`.
- `Tests/PersistenceStackTests.swift` — hermetic tests (always `container(inMemory: true)` +
  `ModelContext`, never the disk store): container construction + model count, schema
  version/model-set + migration-plan construction, round-trip coverage for each of the four
  record types (including nil-optional handling on `CardSRSRecord`), and isolation between two
  separate in-memory containers.

## Checks run

- `swift-format lint --recursive --strict Sources Tests` — pass.
- `swiftlint lint --strict` — 0 violations.
- `xcodegen generate && xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
  — 32 tests, 0 failures (7 new `PersistenceStackTests`).

## Remaining

None for this task. Follow-up tasks T015–T018 add store classes around these records.
