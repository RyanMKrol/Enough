# T002 — Design tokens: colors, typography, layout & motion constants + accent theming

**2026-07-15**

## Built

Implemented the complete DesignSystem token layer per specification:

1. **Colors.swift** — `Color(hex:)` initializer + `EnoughColor` enum with 18 semantic/neutral colors
2. **AccentTheme.swift** — `AccentTheme` enum (japan/france/germany) with computed accent/tint/deep properties + SwiftUI environment key via `@Entry` macro
3. **Typography.swift** — `EnoughFont` enum with 8 semantic-style-backed font helpers + tracking multipliers
4. **Layout.swift** — `Layout` enum with 10 spacing/radius constants
5. **Motion.swift** — `Motion` enum with 8 duration constants + 2 spring animation presets
6. **DesignTokensTests.swift** — XCTest suite with 7 test methods covering:
   - Color hex value equality (5 colors, accuracy ±0.01)
   - AccentTheme color properties (3 themes × accent/tint/deep)
   - AccentTheme raw value initialization
   - Layout/Motion numeric constants
   - Font semantic style equality
   - Default accent theme (.japan)

## Verification

- ✅ Format: `swift-format lint --recursive --strict Sources Tests` passes
- ✅ Lint: `swiftlint lint --strict` passes (0 violations)
- ✅ Tests: `xcodebuild test` — 8/8 tests pass (7 new + 1 existing)
  - All DesignTokensTests assertions pass with computed colors matching expected hex values
  - Default EnvironmentValues.accentTheme confirmed as .japan
  - Font.system() semantic styles properly constructed
- ✅ Build: Xcode project builds clean, no warnings in new code

## Files changed

- Created `Sources/DesignSystem/Colors.swift`
- Created `Sources/DesignSystem/AccentTheme.swift`
- Created `Sources/DesignSystem/Typography.swift`
- Created `Sources/DesignSystem/Layout.swift`
- Created `Sources/DesignSystem/Motion.swift`
- Created `Tests/DesignTokensTests.swift`

All within scope (`Sources/DesignSystem/**`, `Tests/**`). No files outside scope touched.

## Notes

- Used `@MainActor` on test class to handle SwiftUI color isolation (iOS 18 Swift 6 concurrency)
- Color hex initializer tested against computed UIColor RGBA components (0.01 tolerance)
- All type names, property names, and values match spec exactly for later task references
