# T092 — Raise minimum deployment target to iOS 26.0

2026-07-17

## What I did

- `project.yml`: changed `options.deploymentTarget.iOS` from `"18.0"` to `"26.0"`.
- `README.md`: added a `## Requirements` section noting the app targets iOS 26+ (excluding
  iOS 18-25 devices), a deliberate choice for a never-shipped app on current-generation
  iPhones, unblocking iOS 26-only APIs unconditionally.
- Ran `xcodegen generate` and confirmed the generated `Enough.xcodeproj`'s `Enough` and
  `EnoughTests` targets both carry `IPHONEOS_DEPLOYMENT_TARGET = 26.0`.

## Checks run

- `swift-format lint --recursive --strict Sources Tests` — passed, no output.
- `swiftlint lint --strict` — 0 violations across 143 files.
- `xcodebuild test -project Enough.xcodeproj -scheme Enough -destination 'platform=iOS Simulator,id=9481593E-90BC-4051-9F45-01CAE6D17C61'` (Enough-Sim, iOS 26.5) —
  **TEST SUCCEEDED**, 239 tests, 0 failures.

## What remains

Nothing — no source changes were needed, per the task spec.
