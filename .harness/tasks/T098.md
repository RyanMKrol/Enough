## Overview
The actual Home-Screen widget: a TimelineProvider that reads (dueCount, nextDueDate) via ReviewsSummaryProvider off the shared app-group store, with timeline entries predictably derived from the SRS schedule, and a systemSmall view showing the due count / next-due state.

## Do
In Sources/Widget, add a StaticConfiguration widget whose TimelineProvider builds its own ModelContext from PersistenceStack (app-group container) + a widget-bundle ContentStore, constructs ReviewsSummaryProvider, and returns entries: one for 'now', plus follow-up entries scheduled at each upcoming dueAt (capped to a sane number, e.g. next few) so the count increments at the schedule boundaries without a network refresh. Implement placeholder + snapshot. Render systemSmall: when dueCount>0 show the count and a 'reviews due' label; when 0 show 'Nothing due' + 'Next review <relative>' from nextDueDate. Use the design system tokens where available.

## Done when
The widget appears in the widget gallery and systemSmall renders the due count (and the Nothing-due/next-review state) from real SRS data; a unit test asserts the timeline-entry schedule is derived from the summary (an entry at each upcoming dueAt, ordered, count monotonic) using seeded records; xcodebuild test/build stays green; a screenshot of the rendered widget is captured for visual verification.
