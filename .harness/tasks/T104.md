## Overview
Re-attempt of T101, which was BLOCKED for scope-creep: the builder edited build_run.sh (outside the declared single-file scope) while trying to take the verification screenshot. The intended code change was never made — ContinueCard.swift still has showsPercent:false and no wrap guards on its meta line. The scope was correct; the lesson is that build_run.sh must only be INVOKED read-only for the screenshot, never edited. The -demo-state launch flag it needs already exists (Sources/App/LaunchArguments.swift), so no tooling change is required.

## Do
Edit ONLY Sources/Features/Home/ContinueCard.swift. Do NOT edit build_run.sh, LaunchArguments.swift, ProgressViews.swift, or any file other than ContinueCard.swift — the demo-state launch flag and the shared ProgressRing already support everything you need.

1. Show the ring percent: change the ProgressRing(...) call (around line 14-20) from showsPercent: false to showsPercent: true. The shared ProgressRing component (Sources/DesignSystem/ProgressViews.swift) already renders a centered "NN%" label when showsPercent is true — do not touch that component, only this call site.

2. Keep the meta line on one line: the detailLine Text (around line 29-31, e.g. "8 of 30 cards · about 6 min left") currently wraps because the middle text VStack loses width to the trailing Spacer(). Give the middle VStack .layoutPriority(1) so it wins the width it needs over the Spacer, and add .lineLimit(1) plus a modest .minimumScaleFactor(0.85) on the detail Text as a safety net so it never wraps.

Do NOT change the card's gradient, corner radius (24), the 44pt play button, accent theming, or any copy strings. Layout/appearance only, no behaviour change.

To verify visually, INVOKE the existing screenshot script without modifying it: `./build_run.sh -demo-state` (the -demo-state flag is already wired in LaunchArguments.swift and makes the card show real progress). Record what you observed in the worklog.

## Done when
ContinueCard's ProgressRing is constructed with showsPercent: true so the ring shows its percent label. At default text size on the iPhone simulator the Continue card's detail line renders on ONE line with no wrapping (via .layoutPriority(1) on the middle VStack plus .lineLimit(1)/.minimumScaleFactor on the detail Text). The card's gradient, corner radius, play button, accent tint, and all copy strings are unchanged. The git diff touches ONLY Sources/Features/Home/ContinueCard.swift — no other file, and specifically NOT build_run.sh. A ./build_run.sh -demo-state screenshot confirms the ring shows a percent and the meta line is a single line, with the observation recorded in the worklog.
