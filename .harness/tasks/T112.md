## Overview
Re-attempt of T106 (learn session renders a permanent blank white screen so the multiple-choice learn flow is unreachable). T106 BLOCKED because its scope was ONLY Sources/Features/Session/** — the builder concluded the fix also had to touch the fullScreenCover(isPresented:) call sites in HomeView.swift and DeckDetailView.swift (which construct MCSessionView), and correctly refused to edit out-of-scope files rather than trip the structural gate. This re-attempt WIDENS the scope to include both call sites so the presentation path can be fixed end-to-end.

## Do
Make the multiple-choice learn session render instead of a blank white screen, fixing the SwiftUI view-state/presentation bug across BOTH the presented view and its call sites (all four now in scope).

Two interacting problems to resolve:

1) In Sources/Features/Session/MCSessionView.swift, the view holds `@State private var viewModel: MCSessionViewModel?` (nil at first) and only builds it inside `.onAppear` attached to the `else`-branch `Color.white` placeholder — mutating that state from the branch that is about to be swapped out is fragile and can leave `content` stuck on the blank placeholder. Replace this with a state-safe pattern: prefer a NON-optional `@State` view-model built once in `init` (the view already receives `engine`), OR build the view-model in a `.task`/`.onAppear` attached to a STABLE container (e.g. the NavigationStack root), never the branch being conditionally rendered. Note `services` comes from `@Environment` so it is not available in `init`; if you need `services` at construction time, inject it (pass `AppServices` into `MCSessionView.init` from the call sites, or build the view-model in a `.task` where `@Environment` IS available) rather than reintroducing the onAppear-on-else-branch pattern. Do not leave any code path where `viewModel` can stay nil after the view has appeared.

2) At the call sites — `HomeView.startLearnSession` / `HomeView.body`'s `.fullScreenCover(isPresented: $isPresentingLearnSession)` and `DeckDetailView.startLearnSession` / its `.fullScreenCover(isPresented: $isPresentingSession)` — the cover content closures do `if let <engine> { MCSessionView(engine: <engine>) }` against an optional `@State` engine that is set immediately before the presenting flag is flipped. If the presented view is not being constructed (a nil-engine capture the T106 builder flagged), switch these to the value-driven `.fullScreenCover(item:)` form: make the engine state the presentation source of truth (e.g. `SessionEngine` made Identifiable, or wrapped in an Identifiable box) and present with `.fullScreenCover(item: $learnSessionEngine) { engine in MCSessionView(engine: engine) }`, so the cover is never presented with a nil engine and MCSessionView is always constructed with a live engine. Apply the SAME fix to the learn path in DeckDetailView. Do NOT alter the review/practice session presentation behavior except as needed to keep it compiling and working (ReviewSessionView / practice covers must still function).

Keep MCSessionViewModel.init/loadCurrentCard behavior intact — they were verified non-throwing at authoring time; only touch MCSessionViewModel.swift if the chosen construction pattern genuinely requires it (e.g. an injected-services initializer).

## Done when
Starting a multiple-choice learn session renders the REAL session UI — the question header, the tappable answer-option rows, and the SessionChrome (close button + progress bar) — instead of a blank white screen, verified on the simulator from BOTH entry points: (1) the Home / Learn screen Continue-card play button (HomeView.startLearnSession), and (2) the Deck Detail screen's "Continue learning" button (DeckDetailView.startLearnSession). For each path, after tapping, the accessibility identifiers `AXID.screenSessionMC` (`screen-session-mc`) and the session close control (`session-close`) are both present/matchable in the running app (before the fix they never matched), a card's target prompt and answer rows are visible, and tapping the close control dismisses back to the presenting screen. Default launch (no `-demo-state`) with a "New" deck's seeded cards is sufficient. This is a visual-render fix — eyeball it on the simulator, not merely a green build. Format/lint/build all pass and the diff stays within the four scoped files (plus tests).
