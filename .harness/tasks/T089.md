## Overview
Re-attempt of the hidden debug-menu shell + registry. The first attempt failed because the project runs BOTH swift-format --strict (which REQUIRES a trailing comma on the last element of a multiline collection literal) AND swiftlint --strict (whose trailing_comma rule FLAGS that same comma). The spec's example code showed multiline array/collection literals with NO trailing comma on the last element, so swift-format failed; the builder then tried to resolve it by editing .swiftlint.yml (out of the task's scope) and got blocked. This re-attempt bakes in the project's established resolution for that exact conflict.

## Do
Build the hidden debug-menu shell + registry EXACTLY as the original T037 spec describes (DebugRegistry.swift with the verbatim DebugSection/DebugRow/DebugRowKind/DebugProviders types; DebugAbout.swift; DebugMenuView.swift; Sources/App/ShakeDetector.swift; wiring into Sources/App/MainShellView.swift with @State showDebugMenu, .onShake, .sheet, and the -debug-menu launch-argument check in .onAppear; no #if DEBUG; Tests/DebugRegistryTests.swift). Read the original spec at .harness/tasks/T037.md for the full type signatures and behavior — they are a binding contract for dependents T038-T042 and T057. THE ONE CORRECTION: for EVERY multiline collection literal you write (e.g. `DebugProviders.all = [ ... ]`, a section's `rows: [ ... ]`, any gradient/color array), follow this repo's convention — put a trailing comma on the last element AND append `// swiftlint:disable:this trailing_comma` on that same line. This satisfies swift-format --strict (comma present) without tripping swiftlint --strict (rule suppressed for that line), matching the existing pattern already used in Sources/DesignSystem/GradeButtons.swift, Sources/Features/Home/ContinueCard.swift, and Sources/Features/Progress/StreakCard.swift. Do NOT edit .swiftlint.yml, .github/workflows/ci.yml, or any tooling config — the fix lives in the source line, not the linter config. Add a LIMITATIONS row noting the swift-format vs swiftlint trailing-comma conflict and the per-line disable-comment convention as the standing resolution.

## Done when
1) Sources/Features/Debug/DebugRegistry.swift declares the DebugSection, DebugRow, DebugRowKind, and DebugProviders types with the exact names/signatures from .harness/tasks/T037.md (dependents append `Debug<Name>.section` to DebugProviders.all with no further view changes). 2) DebugAbout.swift, DebugMenuView.swift, and Sources/App/ShakeDetector.swift exist; MainShellView presents the sheet on shake and when launched with -debug-menu, with a working Done button. 3) Tests/DebugRegistryTests.swift asserts DebugProviders.all is non-empty, section ids are unique, the 'about' section exists with 2 rows, and row ids within each section are unique — and it passes. 4) The FULL local DoD is green with NO edits to .swiftlint.yml or any tooling config: swift-format lint --recursive --strict Sources Tests, swiftlint lint --strict, and xcodegen generate && xcodebuild test all pass. 5) Launching in the simulator with -debug-menu presents the Debug sheet over the main shell showing the About section (app-version + active-trip rows).
