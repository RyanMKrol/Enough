## Overview
Bake the human-chosen Liquid Glass tab bar variant (from the convert-tabbar-c review) into Sources/DesignSystem/LiquidGlassTabBar.swift as the sole implementation and remove the variant scaffolding.

## Do
Read the convert-tabbar-c worklog for the chosen variant. In Sources/DesignSystem/LiquidGlassTabBar.swift, keep only the chosen variant's glass/selection styling as the bar's single implementation, and delete the variant enum, the `tabBarGlassVariant` constant, and the unused variant branches. Preserve the convert-tabbar-a tap-target fix (`.contentShape(Rectangle())`), the four `EnoughTab` cases with their symbols/titles/axIDs, the accessibility identifiers/labels/traits, and the `#Preview`.

## Done when
The app builds and runs; the bottom tab bar shows only the chosen Liquid Glass treatment with no dead branches or variant switch remaining in the file; all four tabs render, are correctly labelled, and are fully tappable across their whole cell. No leftover variant enum or `tabBarGlassVariant` constant exists in the file.
