## Overview
The floating tab bar (LiquidGlassTabBar.swift, currently named 'LiquidGlass' but faking it with .ultraThinMaterial) and the two deck-detail chrome circle buttons (DeckDetailView.swift back-chevron + ellipsis menu) should use REAL iOS 26 Liquid Glass. Because the app's minimum target is now iOS 26 (liquid-glass-target), apply glassEffect() UNCONDITIONALLY — no #available guard and no .ultraThinMaterial fallback are needed.

## Do
In Sources/DesignSystem/LiquidGlassTabBar.swift, replace the Capsule background's manual .ultraThinMaterial + white overlay + stroke (around lines 36-40) with .glassEffect(.regular, in: Capsule()) so the system provides the glass material directly. In Sources/Features/DeckDetail/DeckDetailView.swift, replace the two Circle() chrome buttons' Color.white.opacity(0.25)/.ultraThinMaterial backing (around lines 34 and 51) with .glassEffect(.regular, in: Circle()), and group the two adjacent glass circles in a GlassEffectContainer so they composite cleanly. Since the minimum deployment target is iOS 26, call glassEffect()/GlassEffectContainer directly with NO #available(iOS 26) guard and NO material else-branch. Keep all layout, sizing, and tap targets unchanged; this is a visual-material swap only. (If needed, consult the axiom-swiftui / axiom-apple-docs skills for the exact glassEffect(_:in:) / GlassEffectContainer(spacing:) signatures rather than guessing.)

## Done when
The floating tab bar renders with .glassEffect(.regular, in: Capsule()) and the two deck-detail circle buttons render with .glassEffect(.regular, in: Circle()) grouped in a GlassEffectContainer, with the old .ultraThinMaterial / manual overlay code removed; there is no #available(iOS 26) guard and no material fallback branch; the app builds against the iOS 26 deployment target and format/lint/test/build all pass; the loop's visual check confirms the tab bar and deck-detail chrome render as real Liquid Glass.
