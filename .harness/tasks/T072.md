## Overview

Add the in-app privacy policy link required by App Review Guideline 5.1.1(i). One row at the
bottom of Browse (the store surface — where policy links are conventionally found).

## Do

1. Read the policy URL from T071's worklog entry (the harness worklog file for T071 — read-only; if it
   is missing, record `failed:blocked` — T071 must complete first).
2. In `Sources/Features/Browse/BrowseView.swift`, add a footer row below the existing "Restore
   purchases" row: a `Link("Privacy policy", destination:)` styled like the restore row
   (TextLink style, 13pt, `#8e8e93` tint acceptable as it duplicates nothing), identifier
   `privacy-policy`.
3. Keep the URL as a single constant (`enum LegalLinks { static let privacyPolicy = URL(...) }`)
   in the same file.

## Done when

- The Browse footer shows "Privacy policy" under "Restore purchases"; tapping opens the URL in
  Safari (verify on the simulator — `-demo-state`, Browse tab, tap the row, Safari opens).
- The link row is present in light of Dynamic Type growth (minHeight, no clipping).
