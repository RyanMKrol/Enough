## Overview

🔒 needs-human. End-to-end acceptance review: a human walks the whole app on the simulator and
judges it against the design intent — the taste call no automated gate can make.

## Do

1. Run `./build_run.sh` (or launch from Xcode) on an iPhone simulator.
2. Walk the full journey twice — once fresh, once seeded:
   - Fresh install: Welcome → country (watch the graphite→accent morph) → trip shape → plan
     (check seeded bundle, savings math, à-la-carte toggles) → purchase → Home.
   - Relaunch with `-demo-state`: Home (streak 6, continue card, 12-due banner) → deck detail →
     an MC learn session incl. one deliberate wrong answer → a flashcard review session →
     session complete → Reviews tab → Progress tab (readiness card) → Browse (buy + restore) →
     debug menu (shake) → time travel +3 days → Reviews shows new dues → new-trip flow to
     France (app reskins blue, packs stay owned).
3. Judge against `.harness/docs/designs/design-spec.md`: tokens, spacing, motion timings,
   haptics on device if available, copy voice (confident & minimal), and the overall "quietly
   joyful, native, no confetti" bar.
4. File anything wrong as new backlog tasks / ideas (`tracking/IDEAS.jsonl`) rather than fixing
   inline.

## Done when

The owner has completed the walkthrough, captured every discrepancy as a backlog idea/task, and
marked this task done via `.harness/scripts/mark-done.sh T060`.
