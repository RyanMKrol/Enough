## Overview

Isolate WHERE the on-disk SwiftData store lives behind one function, so a future Home-Screen
widget (which requires the store in an App Group container) becomes a config change instead of
a user-data migration. Pure seam work — behavior today is unchanged.

## Do

1. In `Sources/Persistence/PersistenceStack.swift`, add
   `static func storeURL(appGroupID: String? = nil) -> URL`:
   - `appGroupID == nil` (today's path): `URL.applicationSupportDirectory` +
     `"Enough/Enough.store"`, creating the intermediate directory if needed.
   - `appGroupID` provided: `FileManager.default.containerURL(forSecurityApplicationGroupIdentifier:)`
     + `"Enough.store"` (fatal-error with a clear message if the entitlement is missing — that's
     a build-config bug, not a runtime condition).
2. Make the on-disk branch of `container(inMemory:)` build its `ModelConfiguration(url:)` from
   `storeURL()` instead of any implicit default location. In-memory behavior unchanged.
3. Add a comment: "Widget adoption: pass the app-group id here AND add a one-time file move from
   the old URL if a store exists there — see the widget task when it lands."
4. Tests (`Tests/StoreURLTests.swift`): `storeURL()` ends in `Enough/Enough.store` under
   Application Support; calling it twice returns the same URL; the directory exists after the
   call. Do NOT test the app-group branch (no entitlement in the test host) — assert only that
   the function signature accepts the parameter.

## Done when

- All on-disk container creation flows through `storeURL()`; grep for `ModelConfiguration(`
  in Sources/ shows the URL always sourced from it (in-memory configs excepted).
- `StoreURLTests` passes; existing persistence tests unchanged and green.
