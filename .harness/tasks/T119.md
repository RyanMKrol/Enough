## Overview
Implement the owned-pack / trip switcher per the decision recorded in convert-trip-packs-switcher-decision. This lets a user who owns multiple packs move between them without destructively re-onboarding. The exact shape (resume saved trips vs start-new; multi-row TripProfileRecord + setActive/allTrips; SRS/Day-N namespacing; UI location) is DEFINED BY the settled decision — this unit implements that decision, it does not re-decide it.

## Do
Read the settled design decision from convert-trip-packs-switcher-decision FIRST and implement exactly that. Depending on the decision this likely means: extend TripStore (Sources/Persistence/TripStore.swift) with allTrips()/setActive(...) and update saveNewTrip/activeTrip semantics; if the decision calls for it, evolve TripProfileRecord (and any SRS/activity namespacing) in Sources/Persistence/Records.swift with an appropriate SwiftData migration; add the switcher UI in the decided location (Learn header in Sources/Features/Home/**, and/or the Browse tab in Sources/Features/Browse/**), offering ONLY packs the user owns (EntitlementStore); and wire the active-trip-dependent surfaces (Home/Progress/Browse/Reviews) to update when the active pack changes. Add tests for the new TripStore API and any migration. NOTE: this unit's precise scope is bounded by the recorded decision — if the decision turns out to require more than one atomic task, split it rather than expanding scope.

## Done when
A user who owns multiple packs can switch which pack they are learning from the decided UI surface, the switcher offers only owned packs, per-pack progress behaves as the recorded decision specifies (resume or fresh), any schema change ships with a migration and tests, the active-trip-dependent screens reflect the switch, and the full Definition of Done passes.
