# T064 — README implementation-status refresh once the core app exists

## Overview

Rewrite the repo's root `README.md` so it documents the app that now exists (the core loop
through session-complete is built by the time this task runs) instead of the pre-implementation
sketch. Scope is `README.md` ONLY. Source material:
.harness/docs/designs/design-spec.md (product truth) and
.harness/docs/designs/architecture.md (module map).

## Do

1. Read the current `README.md` first. PRESERVE, verbatim or lightly reworded: the "Building
   this" section's pointers to .harness/docs/HARNESS.md and CLAUDE.md, and the
   "Development" section's three workflows (xcodegen generate, ./build_run.sh, the
   `LOCAL_DOD` pointer in .harness/config/harness.env). Everything else may be rewritten.
2. Rewrite the top as one tight paragraph: Enough is a native SwiftUI iOS app that teaches just
   enough of a language for a short trip — scenario-sized packs (£1 each, or discounted trip
   bundles; one-time purchases, stubbed StoreKit for now) learned through an Anki-style SM-2
   spaced-repetition engine. Correct the stale "£3 each" claim to the real pricing model.
3. Add a "What's implemented" section (prose + short bullets, no per-task table): onboarding
   (country → trip shape → plan/purchase), the four-tab shell (Learn / Reviews / Progress /
   Browse), multiple-choice learn sessions + tap-to-reveal flashcard reviews driving the SM-2
   schedule, streaks & survival readiness, review-due local notifications (if built by then),
   and the debug god-mode menu — including the launch arguments `-debug-menu` (open the menu)
   and `-demo-state` (wipe + seed a mid-trip demo fixture, used by build_run.sh
   screenshots). State plainly anything from that list that is NOT yet merged at the time of
   writing (check .harness/tracking/TASKS.json statuses) rather than overclaiming.
4. Add a compact "Architecture" section mirroring architecture.md's module table in ~8 lines:
   Sources/App (shell/DI), CoreKit (clock), DesignSystem (tokens + components),
   ContentKit (catalog models/loader), Persistence (SwiftData records + stores), SRSKit
   (pure SM-2 + session engine), Services (study/purchases/audio/stats/notifications glue),
   Features/<Area> (screens) — with a pointer to .harness/docs/designs/architecture.md for
   signatures and .harness/docs/designs/design-spec.md for design truth.
5. Add a short "Content" section: decks/cards/audio are bundled data under `Content/`
   (catalog.json, decks/*.json, audio/*.mp3) in the schema of design-spec §8.1, produced
   by the external anki-builder pipeline; current content is a placeholder fixture (Japanese
   textbook slices; France/Germany reuse the Japanese files).
6. Replace the old "Implementation status" task table and the stale "Status" section with one
   "Status" paragraph pointing at .harness/tracking/TASKS.json (and the harness dashboard)
   as the live backlog record. Mention tests run via the `LOCAL_DOD` xcodebuild invocation.

## Done when

- `README.md` accurately describes the implemented app: no "£3", no "placeholder screen / no
  product features yet", no claims about features whose tasks aren't `done` in
  .harness/tracking/TASKS.json at build time.
- The harness pointers (HARNESS.md, CLAUDE.md) and the three Development workflows survive.
- Both launch arguments (`-debug-menu`, `-demo-state`) are documented.
- The diff touches only `README.md`.
