## Overview
Re-attempt of T050: build SessionCompleteView and swap it in for the placeholder `Text("Complete")` in both MCSessionView and ReviewSessionView. The first attempt was BLOCKED for scope-creep — it edited Sources/Features/DeckDetail/DeckDetailView.swift, outside the declared Session scope, and the loop aborts at the first out-of-scope edit with no escalation. That edit was UNNECESSARY: dismissal is already available inside the session views themselves.

## Do
Do NOT touch any presenting screen — not DeckDetailView, HomeView, or ReviewsTabView. They present the session inside a `.fullScreenCover`, but the session views (MCSessionView, ReviewSessionView) each already hold `@Environment(\.dismiss) private var dismiss` (MCSessionView already calls `dismiss()` from SessionChrome's onClose). So `SessionCompleteView`'s `onDone` closure MUST be wired at the `.navigationDestination(for: SessionRoute.self)` site inside each session view as `onDone: { dismiss() }` — that dismisses the full-screen cover back to whatever launched it, with zero changes to any launcher. Keep every edit under Sources/Features/Session/** (and Tests/** if you add tests).

Work to do:
1. Create `Sources/Features/Session/SessionCompleteView.swift` — `struct SessionCompleteView: View` with `init(summary: SessionSummary, deckId: String?, onDone: @escaping () -> Void)` (deckId = the session's deck for learn/practice; nil for review sessions). Centered VStack on white:
   - Hero (ZStack ~130pt): `ProgressRing(progress: 1.0, size: 120, lineWidth: 8, tint: accentTheme.accent, showsPercent: false)` (its built-in fill animation races to full on appear) with `CheckPopView(size: 44, color: accentTheme.accent)` overlaid, delayed 0.3s via `.task { try? await Task.sleep(for: .seconds(0.3)); showCheck = true }`.
   - Title "Round complete" — 30 Bold, top padding 18.
   - Subline 15 Regular #6b6b70, centered, max-width 300:
     - review mode: "You cleared today's reviews. \(strongestDeckTitle) is holding strong." — strongest = OWNED deck with highest `DeckProgressService.strength`; tie-break: most `mastered`; titles via ContentStore.
     - learn/practice mode: "That's \(deckTitle) under way. \(summary.cardsCompleted) cards down."
   - Three stat tiles (HStack spacing 10; each white card radius 18, hairline border, padding 14, equal widths): value 22 Bold + caption 12 Regular #8e8e93 — "\(summary.cardsCompleted)"/"cards"; `summary.duration` formatted m:ss (e.g. "4:10")/"minutes"; "\(streak)"/"day streak" where streak = `StatsService.currentStreak()` read AT APPEAR TIME.
   - "Done" — `PrimaryButtonStyle(background: accentTheme.accent)` → `onDone()`.
   - "Learn 5 more" — `TextLinkButtonStyle` — shown ONLY when `services.study.makeLearnMoreSession(size: 5)` yields a non-empty engine; tapping replaces the current session content with `MCSessionView(engine:)` for that engine.
   - Motion: ring fills once, check pops once — nothing loops, no confetti.
   - Keep the `.accessibilityIdentifier(AXID.screenSessionComplete)` currently on the placeholder.
2. In BOTH `MCSessionView` and `ReviewSessionView`, replace the placeholder `Text("Complete")` in the `.navigationDestination` switch's `.complete` case with `SessionCompleteView(summary: engine.summary(), deckId: <deck or nil>, onDone: { dismiss() })`. If a shared destination helper avoids duplication, put it in `Sources/Features/Session/SessionRoute.swift` (still in scope). Remove the `// TODO(T050)` and the `swiftlint:disable:next todo` lines.
3. Add #Previews: review variant (12 cards, 4:10, streak 7) and learn variant.

Copy strings EXACT: "Round complete", "Done", "Learn 5 more", "You cleared today's reviews. \(deck) is holding strong.", "That's \(deck) under way. \(n) cards down.".

## Done when
- `Sources/Features/Session/SessionCompleteView.swift` exists; the placeholder `Text("Complete")` (and its TODO(T050) comment) is gone from BOTH MCSessionView and ReviewSessionView.
- NO file outside Sources/Features/Session/** (or Tests/**) is modified — in particular DeckDetailView.swift, HomeView.swift, and ReviewsTabView.swift are untouched; `onDone` is wired via the session view's own `@Environment(\.dismiss)`.
- Finishing an MC learn session or a flashcard review session lands on SessionCompleteView; review variant names the strongest owned deck, learn variant names the studied deck and card count; duration renders m:ss; the streak tile reflects the just-committed session.
- "Learn 5 more" appears only when new cards remain and launches a 5-card MC session; "Done" dismisses the full-screen cover back to the launching screen.
- Ring + check animate exactly once on appear; nothing loops.
- swift-format lint, swiftlint --strict, and the xcodebuild test/build all pass.
- Visual check on the simulator: completing a short session shows the full 120pt ring with popped check, "Round complete", three stat tiles, and both actions, matching design-spec §6c.
