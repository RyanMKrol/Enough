## Overview
Re-attempt of T100, which was BLOCKED for scope-creep: the first attempt's diff touched Sources/EnoughApp.swift and .swiftlint.yml, both outside the declared scope (Sources/App/**, Sources/Services/**, Tests/**). Sources/EnoughApp.swift is the app's @main entry point (it lives at Sources/, NOT under Sources/App/), and registering the AppShortcutsProvider plus bridging the AppIntent to the live AppState instance genuinely requires editing it. This re-attempt widens scope to include Sources/EnoughApp.swift and names the app-state-access wiring the first attempt tripped over.

## Do
Add a 'Start my review' App Intent, in the MAIN app target (no separate extension target, no new entitlements). Concretely:

1. Add an AppIntent type (e.g. StartReviewIntent) under Sources/App/ with a clear localized title, static openAppWhenRun = true, and a perform() that runs on the main actor. perform() must route through the EXISTING shared hook AppState.requestStartReview() (already added by T099 in Sources/App/AppState.swift; it sets pendingAction = .startReview, which MainShellView already observes to switch to the Reviews tab and start a session via StudyService) and then return .result().

2. Solve the app-state-access problem that forced the first attempt outside scope: AppState is currently instantiated per-WindowGroup inside EnoughApp with NO static/shared accessor, so the intent's perform() has no handle on the live instance. Add a minimal bridge — the simplest correct option is a static/shared reference to the live AppState (e.g. a static weak or nonisolated(unsafe)-free MainActor-isolated accessor set when EnoughApp constructs AppState in init/body). Wire EnoughApp (Sources/EnoughApp.swift) to publish that reference so the intent can call requestStartReview() on it. Keep this bridge tiny and MainActor-isolated; do NOT introduce a second AppState.

3. Register the intent with an AppShortcutsProvider (its appShortcuts static var) exposing a natural-language phrase such as 'Start my review in ${applicationName}' / 'Start my review in Enough' so it surfaces in the Shortcuts app and Spotlight.

4. Do NOT edit .swiftlint.yml — the previous attempt's edit to it was gratuitous. New source files under Sources/App/ are already covered by the linter's globs and must simply pass swift-format + swiftlint --strict as-is. Ensure everything compiles under the app target's Swift 6 / MainActor isolation.

5. Add a unit test (Tests/) asserting the intent's perform() wiring invokes the shared start-review hook — e.g. drive perform() (or the bridge it calls) against a test AppState and assert pendingAction becomes .startReview.

## Done when
StartReviewIntent and its AppShortcut are registered so the phrase appears in the Shortcuts app / Spotlight; invoking the intent opens the app, switches to the Reviews tab, and starts a review session through the existing AppState.requestStartReview() -> pendingAction -> MainShellView -> StudyService path (no new StudyService entry point); a unit test asserts perform() calls the shared start-review hook (pendingAction == .startReview); .swiftlint.yml is unchanged; and the full local Definition-of-Done (swift-format lint --strict, swiftlint --strict, xcodegen generate && xcodebuild test) stays green.
