## Overview
One-time human provisioning step: choose the App Group identifier and enable the App Groups capability in the Apple Developer portal / Xcode signing so both the app and the (upcoming) widget extension can share the SwiftData store. Nothing above it can read the shared store until this is done.

## Do
The App Group id is group.com.ryankrol.enough (chosen by the owner). Register it and enable the App Groups capability for both the Enough app target and the EnoughWidget extension target's bundle ids in the Apple Developer portal. Add the two .entitlements files (Sources/Enough.entitlements and Sources/Widget/EnoughWidget.entitlements) each declaring com.apple.security.application-groups with group.com.ryankrol.enough, and wire CODE_SIGN_ENTITLEMENTS to them in project.yml for the respective targets. Record group.com.ryankrol.enough in .harness/custom/docs/LIMITATIONS.md so downstream buildable tasks read the id from there.

## Done when
The App Group id group.com.ryankrol.enough is recorded in .harness/custom/docs/LIMITATIONS.md; both .entitlements files exist declaring that group; project.yml points each target's CODE_SIGN_ENTITLEMENTS at its file; the human confirms the capability is enabled in the Developer portal for both bundle ids. (needs-human: do not auto-complete — record failed:blocked with everything else prepared.)
